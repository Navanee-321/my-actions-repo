{
  "name": "your-project-name",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "test": "npm run lint && npm run test:unit",
    "build": "your-build-command-here",  // <-- Add this line
    "lint": "eslint .",
    "test:unit": "jest"
  },
  "dependencies": {
    "express": "^4.17.1",
    "jest": "^26.6.0"
  }
}

